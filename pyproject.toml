[tool.ruff]
line-length = 100
target-version = "py313"
extend-exclude = ["alembic/versions"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]  # Depends()/Query() en valeur par defaut = pattern FastAPI

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
